# 통리반 검색키 자동 생성 안내

## 목적

`tongban.csv`의 원문 관할구역은 그대로 두고, 도로명주소 DB에서 안전하게 확인되는 건물명을 `검색키` 컬럼으로 자동 추가합니다.

이 방식은 다음 원칙을 따릅니다.

- 지번으로 통리반이 먼저 확인되는 경우만 자동 검색키를 추가합니다.
- 건물명이 비슷하다는 이유만으로는 자동 연결하지 않습니다.
- 사람이 읽는 `관할구역` 문구는 유지하고, 검색 보조용 `검색키` 컬럼만 추가합니다.

## 실행 순서

프로젝트 루트에서 실행합니다.

```bash
python scripts/generate_tongban_search_keys.py
python scripts/build_data.py
```

실행 후 생성되는 파일:

```text
source_data/tongban_with_search_keys.csv
source_data/tongban_search_key_report.csv
public/data/core.json
public/data/roads.json
public/data/suggestions.json
```

## GitHub에 올릴 파일

일반적으로 아래 파일을 반영하면 됩니다.

```text
public/main.js
scripts/build_data.py
scripts/generate_tongban_search_keys.py
source_data/tongban_with_search_keys.csv
```

그리고 `python scripts/build_data.py` 실행 후 생성된:

```text
public/data/core.json
public/data/roads.json
public/data/suggestions.json
```

도 함께 반영합니다.

## 직접 수정이 필요한 경우

예를 들어 도로명주소 DB 건물명은 `동탄역 롯데캐슬알바트로스`인데 원자료에는 `A28블록 롯데캐슬 아파트`만 있는 경우, 지번으로 자동 연결되지 않으면 자동 검색키에 들어가지 않을 수 있습니다.

이때는 `source_data/tongban_with_search_keys.csv`의 `검색키` 컬럼에 수동으로 다음처럼 추가하세요.

```text
동탄역 롯데캐슬알바트로스
```

여러 개는 세미콜론으로 구분합니다.

```text
동탄역 롯데캐슬알바트로스; 동탄순환대로21길 53
```

그다음 다시:

```bash
python scripts/build_data.py
```

을 실행하면 반영됩니다.

## 주의

검색키 자동 생성기는 기본적으로 건물명만 자동 추가합니다. 도로명주소나 지번주소까지 모두 검색키에 넣으면 일부 통리반 행이 너무 길어질 수 있기 때문입니다.

필요할 때만 아래 옵션을 사용하세요.

```bash
python scripts/generate_tongban_search_keys.py --include-road-addresses
python scripts/generate_tongban_search_keys.py --include-jibun-addresses
```
