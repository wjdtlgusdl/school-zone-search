# 통학구역 조회 v4 풀 패키지 적용 안내

## 교체/추가 파일

GitHub 프로젝트에 아래 파일을 덮어쓰면 됩니다.

- `public/main.js`
- `public/data/core.json`
- `public/data/search_index.json` ← 새 파일
- `public/data/roads.json`
- `public/data/suggestions.json`
- `scripts/build_data_v4.py`
- `scripts/audit.py`
- 필요 시 `scripts/generate_tongban_search_keys.py`, `scripts/build_data.py`
- `source_data/building_alias.csv`
- `source_data/address_rules.csv`

## 앞으로 데이터 수정 흐름

기존처럼 검색키를 다시 생성해야 하는 경우:

```bash
python scripts/generate_tongban_search_keys.py
python scripts/build_data.py
python scripts/build_data_v4.py
```

핵심은 마지막 `build_data_v4.py`입니다.  
이 파일이 기존 `core.json`의 검색키를 `search_index.json`으로 분리합니다.

## 특정 주소가 검색되지 않을 때

코드부터 고치지 말고 아래 순서로 봅니다.

1. 도로명주소가 `roads.json`에 있는지 확인
2. 지번/건물명이 `search_index.json`에 있는지 확인
3. 없으면 `building_alias.csv` 또는 `address_rules.csv`에 보정 규칙 추가
4. 파이썬 3개 실행
5. `public/data/*.json` 업로드

## 품질검사

```bash
python scripts/audit.py
```

결과는 `source_data/audit_result.csv`에 생성됩니다.

## v4 구조

- `core.json`: 통리반/학교 정보만 보관
- `search_index.json`: 검색키 → 통리반 id 연결만 보관
- `roads.json`: 도로명주소 → 지번/건물명 변환

이 구조에서는 새 예외가 생겨도 대부분 CSV나 규칙 파일만 수정하면 되고, `main.js`는 거의 건드리지 않아도 됩니다.
